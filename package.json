{
  "name": "iron-requiem-3d-remaster",
  "version": "0.3.0",
  "private": true,
  "description": "IRON REQUIEM 3D REMASTER - Arc-12 Campaign Expansion",
  "type": "module",
  "scripts": {
    "install:all": "node scripts/install-all.js",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build:client": "npm --prefix client run build",
    "start:server": "npm --prefix server run start",
    "check": "node scripts/run-tests.js && npm --prefix server test && npm --prefix client run build"
  },
  "engines": {
    "node": ">=20"
  }
}
